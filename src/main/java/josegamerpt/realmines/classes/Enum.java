package josegamerpt.realmines.classes;

public class Enum {
	
	public enum Data {BLOCKS, ICON, ALL, TELEPORT, SIGNS, REGION, INIT, OPTIONS}
	public enum PickType {ICON, BLOCK}
	public enum Reset {PERCENTAGE, TIME}
}
